# GiftLink capstone backlog

Refer to `PRODUCT_BACKLOG.md` for the current user stories, label definitions (`new`, `backlog`, `icebox`, `technical debt`), and triage decisions. The `.github/ISSUE_TEMPLATE/user-story.md` template now applies the `new` label by default so stories can be quickly refined into the backlog or icebox.
